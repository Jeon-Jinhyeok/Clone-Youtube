extends base

block content
    h4 Change Title of Video
    form(, method="post")
        input(, name="title", placeholder="New Title", type="text", value=video.title, required)
        input(value="Save", type="submit")